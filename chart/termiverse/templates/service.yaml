apiVersion: v1
kind: Service
metadata:
  name: {{ include "termiverse.fullname" . }}
  labels:
    {{- include "termiverse.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "termiverse.selectorLabels" . | nindent 4 }}
