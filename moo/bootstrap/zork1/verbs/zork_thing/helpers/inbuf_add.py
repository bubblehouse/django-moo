#!moo verb inbuf_add --on "Zork Thing" --dspec this
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison,unnecessary-pass,expression-not-assigned

from moo.sdk import context
# ZIL routine: INBUF-ADD
# params: LEN, BEG, SLOT
# aux: DBEG, CTR, TMP

player = context.player
len_v = args[0] if len(args) > 0 else None
beg = args[1] if len(args) > 1 else None
slot = args[2] if len(args) > 2 else None
dbeg = 0
ctr = 0
tmp = 0

if tmp := _.table_get(player.zstate_get("OOPS-TABLE"), player.zstate_get("O-END")):
    dbeg = tmp
else:
    dbeg = _.table_get(
        player.zstate_get("AGAIN-LEXV"),
        (tmp := _.table_get(player.zstate_get("OOPS-TABLE"), player.zstate_get("O-LENGTH"))),
    ) + _.table_get(player.zstate_get("AGAIN-LEXV"), tmp + 1)
# ZIL: <PUT ...>
_.table_put(player.zstate_get("OOPS-TABLE"), player.zstate_get("O-END"), dbeg + len_v)
while True:
    # ZIL: <PUTB ...>
    putb(player.zstate_get("OOPS-INBUF"), dbeg + ctr, _.table_get(player.zstate_get("P-INBUF"), beg + ctr))
    ctr = ctr + 1
    if ctr == len_v:
        break
# ZIL: <PUTB ...>
putb(player.zstate_get("AGAIN-LEXV"), slot, dbeg)
# ZIL: <PUTB ...>
return putb(player.zstate_get("AGAIN-LEXV"), slot - 1, len_v)
