#!moo verb pre_board --on "Zork Thing" --dspec this
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison,unnecessary-pass,expression-not-assigned

from moo.sdk import context, lookup
# ZIL routine: PRE-BOARD
# aux: AV

player = context.player
parser = context.parser
av = 0

av = player.location
if _.zork_thing.null_f():
    return True
elif parser.get_dobj().flag("vehicle"):
    if not parser.get_dobj().location == player.here():
        print("The " + parser.get_dobj().desc() + " must be on the ground to be boarded.")
    elif av.flag("vehicle"):
        print("You are already in the " + av.desc() + "!")
    else:
        return False
elif parser.get_dobj() in (lookup("water"), lookup("global_water")):
    _.perform(player.zstate_get("V?SWIM"), parser.get_dobj(), None)
    return True
else:
    print("You have a theory on how to board a " + parser.get_dobj().desc() + ", perhaps?")
# ZIL: <RFATAL ...>
return 2
