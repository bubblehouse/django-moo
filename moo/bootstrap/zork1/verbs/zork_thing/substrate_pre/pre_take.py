#!moo verb pre_take --on "Zork Thing" --dspec this
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison,unnecessary-pass,expression-not-assigned

from moo.sdk import context, lookup
# ZIL routine: PRE-TAKE

player = context.player
parser = context.parser

if parser.get_dobj().location == player:
    if parser.get_dobj().flag("wearbit"):
        print("You are already wearing it.")
        return
    else:
        print("You already have that!")
        return
elif parser.get_dobj().location.flag("contbit") and not parser.get_dobj().location.flag("open"):
    print("You can't reach something that's inside a closed container.")
    return True
elif parser.get_iobj() if parser.has_iobj() else None:
    if (parser.get_iobj() if parser.has_iobj() else None) == lookup("ground"):
        # SETG of parser-state slot is a no-op in DjangoMOO
        return False
    if player.zstate_get("ZORK-NUMBER") == 2:
        if parser.get_dobj() == player.zstate_get("DOOR-KEEPER"):
            # SETG of parser-state slot is a no-op in DjangoMOO
            return False
    else:
        # ZIL: <NULL-F ...>
        return _.zork_thing.null_f()
    if not (parser.get_iobj() if parser.has_iobj() else None) == parser.get_dobj().location:
        print(
            "The "
            + parser.get_dobj().desc()
            + " isn't in the "
            + (parser.get_iobj() if parser.has_iobj() else None).desc()
            + "."
        )
        return
    else:
        # SETG of parser-state slot is a no-op in DjangoMOO
        return False
elif parser.get_dobj() == player.location:
    print("You're inside of it!")
    return
