#!moo verb steal_junk --on "Zork Thing" --dspec this
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison,unnecessary-pass,expression-not-assigned

import random
from moo.sdk import context, lookup
# ZIL routine: STEAL-JUNK
# params: RM
# aux: X, N

player = context.player
rm = args[0] if len(args) > 0 else None
x = 0
n = 0

x = rm.contents.first()
while True:
    if not x:
        return False
    n = _.next_sibling(x)
    if (
        x.getp("tvalue") == 0
        and x.flag("takeable")
        and not x.flag("sacred")
        and not x.flag("invisible")
        and x == lookup("stiletto")
        or random.randint(1, 100) <= 10
    ):
        x.moveto(lookup("thief"))
        x.set_flag("touchbit", True)
        x.set_flag("invisible", True)
        if x == lookup("rope"):
            player.zstate_set("DOME-FLAG", None)
        if rm == player.here():
            print("You suddenly notice that the " + x.desc() + " vanished.")
            return True
        else:
            return False
    x = n
