#!moo verb overboard --on "Zork Thing" --dspec this
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison,unnecessary-pass,expression-not-assigned

from moo.sdk import context, lookup
# ZIL routine: V-OVERBOARD
# aux: LOCN

player = context.player
parser = context.parser
locn = 0

if (parser.get_iobj() if parser.has_iobj() else None) == lookup("teeth"):
    if (locn := player.location).flag("vehicle"):
        parser.get_dobj().moveto(locn.location)
        print("Ahoy -- " + parser.get_dobj().desc() + " overboard!")
        return
    else:
        print("You're not in anything!")
        return
elif player.location.flag("vehicle"):
    _.perform(player.zstate_get("V?THROW"), parser.get_dobj(), None)
    return True
else:
    print("Huh?")
    return
