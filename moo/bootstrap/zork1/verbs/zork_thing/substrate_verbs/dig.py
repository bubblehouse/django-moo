#!moo verb dig --on "Zork Thing" --dspec this
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison,unnecessary-pass,expression-not-assigned

from moo.sdk import context, lookup
# ZIL routine: V-DIG

player = context.player
parser = context.parser

if not (parser.get_iobj() if parser.has_iobj() else None):
    # SETG of parser-state slot is a no-op in DjangoMOO
    pass
if player.zstate_get("ZORK-NUMBER") == 1:
    if (parser.get_iobj() if parser.has_iobj() else None) == lookup("shovel"):
        print("There's no reason to be digging here.")
        return True
else:
    return True
if (parser.get_iobj() if parser.has_iobj() else None).flag("toolbit"):
    print("Digging with the " + (parser.get_iobj() if parser.has_iobj() else None).desc() + " is slow and tedious.")
    return
else:
    print("Digging with a " + (parser.get_iobj() if parser.has_iobj() else None).desc() + " is silly.")
    return
