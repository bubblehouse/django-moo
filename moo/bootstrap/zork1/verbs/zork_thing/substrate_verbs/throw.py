#!moo verb throw --on "Zork Thing" --dspec this
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison,unnecessary-pass,expression-not-assigned

from moo.sdk import context, lookup
# ZIL routine: V-THROW

player = context.player
parser = context.parser

if _.zork_thing.idrop():
    if (parser.get_iobj() if parser.has_iobj() else None) == lookup("me"):
        print("A terrific throw! The " + parser.get_dobj().desc(), end="")
        player.zstate_set("WINNER", player)
        return _.jigs_up(
            " hits you squarely in the head. Normally,\nthis wouldn't do much damage, but by incredible mischance, you fall over\nbackwards trying to duck, and break your neck, justice being swift and\nmerciful in the Great Underground Empire."
        )
    elif (parser.get_iobj() if parser.has_iobj() else None) and (parser.get_iobj() if parser.has_iobj() else None).flag(
        "actorbit"
    ):
        print(
            "The "
            + (parser.get_iobj() if parser.has_iobj() else None).desc()
            + " ducks as the "
            + parser.get_dobj().desc()
            + " flies by and crashes to the ground."
        )
        return
    else:
        print("Thrown.")
        return
else:
    print("Huh?")
    return
