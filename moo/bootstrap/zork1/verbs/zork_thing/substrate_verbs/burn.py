#!moo verb burn --on "Zork Thing" --dspec this
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison,unnecessary-pass,expression-not-assigned

from moo.sdk import context
# ZIL routine: V-BURN

player = context.player
parser = context.parser

pre_x = "pre-burn"
if _.zork_thing.has_verb(pre_x) and _.zork_thing.invoke_verb(pre_x):
    return
if _.zork_thing.null_f():
    return False
elif parser.get_dobj().flag("flammable"):
    if parser.get_dobj().location == player or player.location == parser.get_dobj():
        _.remove(parser.get_dobj())
        print("The " + parser.get_dobj().desc() + " catches fire. Unfortunately, you were ", end="")
        if player.location == parser.get_dobj():
            print("in", end="")
        else:
            print("holding", end="")
        return _.jigs_up(" it at the time.")
    else:
        _.remove(parser.get_dobj())
        print("The " + parser.get_dobj().desc() + " catches fire and is consumed.")
        return
else:
    print("You can't burn a " + parser.get_dobj().desc() + ".")
    return
