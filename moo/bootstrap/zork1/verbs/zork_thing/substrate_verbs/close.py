#!moo verb close --on "Zork Thing" --dspec this
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison,unnecessary-pass,expression-not-assigned

from moo.sdk import context
# ZIL routine: V-CLOSE

player = context.player
parser = context.parser

if not parser.get_dobj().flag("contbit") and not parser.get_dobj().flag("is_door"):
    print("You must tell me how to do that to a " + parser.get_dobj().desc() + ".")
    return
elif not parser.get_dobj().flag("surfacebit") and not parser.get_dobj().getp("capacity") == 0:
    if parser.get_dobj().flag("open"):
        parser.get_dobj().set_flag("open", False)
        print("Closed.")
        if player.zstate_get("LIT") and not player.zstate_set("LIT", _.zork_thing.is_lit(player.here())):
            print("It is now pitch black.")
        return True
    else:
        print("It is already closed.")
        return
elif parser.get_dobj().flag("is_door"):
    if parser.get_dobj().flag("open"):
        parser.get_dobj().set_flag("open", False)
        print("The " + parser.get_dobj().desc() + " is now closed.")
        return
    else:
        print("It is already closed.")
        return
else:
    print("You cannot close that.")
    return
