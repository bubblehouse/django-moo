#!moo verb open --on "Zork Thing" --dspec this
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison,unnecessary-pass,expression-not-assigned

from moo.sdk import context
# ZIL routine: V-OPEN
# aux: F, STR

parser = context.parser
f = 0
str_v = 0

if parser.get_dobj().flag("contbit") and not parser.get_dobj().getp("capacity") == 0:
    if parser.get_dobj().flag("open"):
        print("It is already open.")
        return
    else:
        parser.get_dobj().set_flag("open", True)
        parser.get_dobj().set_flag("touchbit", True)
        if not parser.get_dobj().contents.first() or parser.get_dobj().flag("transparent"):
            print("Opened.")
            return
        elif (
            (f := parser.get_dobj().contents.first())
            and not _.next_sibling(f)
            and not f.flag("touchbit")
            and (str_v := f.getp("description"))
        ):
            print("The " + parser.get_dobj().desc() + " opens.")
            print(str_v)
            return
        else:
            print("Opening the " + parser.get_dobj().desc() + " reveals ", end="")
            # ZIL: <PRINT-CONTENTS ...>
            _.zork_thing.print_contents(parser.get_dobj())
            print(".")
            return
elif parser.get_dobj().flag("is_door"):
    if parser.get_dobj().flag("open"):
        print("It is already open.")
        return
    else:
        print("The " + parser.get_dobj().desc() + " opens.")
        return parser.get_dobj().set_flag("open", True)
else:
    print("You must tell me how to do that to a " + parser.get_dobj().desc() + ".")
    return
