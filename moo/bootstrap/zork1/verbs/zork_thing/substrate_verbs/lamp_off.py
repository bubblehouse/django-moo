#!moo verb lamp_off --on "Zork Thing" --dspec this
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison,unnecessary-pass,expression-not-assigned

from moo.sdk import context
# ZIL routine: V-LAMP-OFF

player = context.player
parser = context.parser

if parser.get_dobj().flag("lit"):
    if not parser.get_dobj().flag("onbit"):
        print("It is already off.")
    else:
        parser.get_dobj().set_flag("onbit", False)
        if player.zstate_get("LIT"):
            player.zstate_set("LIT", _.zork_thing.is_lit(player.here()))
        print("The " + parser.get_dobj().desc() + " is now off.")
        if not player.zstate_get("LIT"):
            print("It is now pitch black.")
else:
    print("You can't turn that off.")
return True
