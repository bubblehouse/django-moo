#!moo verb pour_on --on "Zork Thing" --dspec this
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison,unnecessary-pass,expression-not-assigned

from moo.sdk import context, lookup
# ZIL routine: V-POUR-ON

player = context.player
parser = context.parser
putty = lookup("putty")

if parser.get_dobj() == lookup("water"):
    _.remove(parser.get_dobj())
    if flaming_p((parser.get_iobj() if parser.has_iobj() else None)):
        print("The " + (parser.get_iobj() if parser.has_iobj() else None).desc() + " is extinguished.")
        if player.zstate_get("ZORK-NUMBER") == 2:
            if (parser.get_iobj() if parser.has_iobj() else None) == player.zstate_get("BINF-FLAG"):
                player.zstate_set("BINF-FLAG", None)
        else:
            # ZIL: <NULL-F ...>
            _.zork_thing.null_f()
        (parser.get_iobj() if parser.has_iobj() else None).set_flag("onbit", False)
        return (parser.get_iobj() if parser.has_iobj() else None).set_flag("flamebit", False)
    else:
        print(
            "The water spills over the "
            + (parser.get_iobj() if parser.has_iobj() else None).desc()
            + ", to the floor, and evaporates."
        )
        return
elif parser.get_dobj() == putty:
    return _.perform(player.zstate_get("V?PUT"), putty, (parser.get_iobj() if parser.has_iobj() else None))
else:
    print("You can't pour that.")
    return
