#!moo verb cut --on "Zork Thing" --dspec this
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison,unnecessary-pass,expression-not-assigned

from moo.sdk import context
# ZIL routine: V-CUT

player = context.player
parser = context.parser

if parser.get_dobj().flag("actorbit"):
    return _.perform(
        player.zstate_get("V?ATTACK"), parser.get_dobj(), (parser.get_iobj() if parser.has_iobj() else None)
    )
elif parser.get_dobj().flag("flammable") and (parser.get_iobj() if parser.has_iobj() else None).flag("weapon"):
    if player.location == parser.get_dobj():
        print("Not a bright idea, especially since you're in it.")
        return True
    _.remove(parser.get_dobj())
    print(
        "Your skillful "
        + (parser.get_iobj() if parser.has_iobj() else None).desc()
        + "smanship slices the "
        + parser.get_dobj().desc()
        + " into innumerable slivers which blow away."
    )
    return
elif not (parser.get_iobj() if parser.has_iobj() else None).flag("weapon"):
    print(
        'The "cutting edge" of a ' + (parser.get_iobj() if parser.has_iobj() else None).desc() + " is hardly adequate."
    )
    return
else:
    print("Strange concept, cutting the " + parser.get_dobj().desc() + "....")
    return
