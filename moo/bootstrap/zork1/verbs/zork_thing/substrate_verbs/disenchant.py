#!moo verb disenchant --on "Zork Thing" --dspec this
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison,unnecessary-pass,expression-not-assigned

from moo.sdk import context
# ZIL routine: V-DISENCHANT

player = context.player
parser = context.parser

if player.zstate_get("ZORK-NUMBER") == 2:
    if not parser.get_dobj().location == player.here():
        return True
    elif (
        player.zstate_get("SPELL-USED")
        in (player.zstate_get("W?FEEBLE"), player.zstate_get("W?FUMBLE"), player.zstate_get("W?FEAR"))
        or player.zstate_get("SPELL-USED")
        in (player.zstate_get("W?FREEZE"), player.zstate_get("W?FALL"), player.zstate_get("W?FERMENT"))
        or player.zstate_get("SPELL-USED")
        in (player.zstate_get("W?FIERCE"), player.zstate_get("W?FENCE"), player.zstate_get("W?FANTASIZE"))
    ):
        if parser.get_dobj().flag("actorbit"):
            if player.zstate_get("SPELL-USED") == player.zstate_get("W?FEEBLE"):
                print("The " + parser.get_dobj().desc() + " seems stronger now.")
                return
            elif player.zstate_get("SPELL-USED") == player.zstate_get("W?FUMBLE"):
                print("The " + parser.get_dobj().desc() + " no longer appears clumsy.")
                return
            elif player.zstate_get("SPELL-USED") == player.zstate_get("W?FEAR"):
                print("The " + parser.get_dobj().desc() + " no longer appears afraid.")
                return
            elif player.zstate_get("SPELL-USED") == player.zstate_get("W?FREEZE"):
                print("The " + parser.get_dobj().desc() + " moves again.")
                return
            elif player.zstate_get("SPELL-USED") == player.zstate_get("W?FERMENT"):
                print("The " + parser.get_dobj().desc() + " stops swaying.")
                return
            elif player.zstate_get("SPELL-USED") == player.zstate_get("W?FIERCE"):
                print("The " + parser.get_dobj().desc() + " appears more peaceful.")
                return
    elif player.zstate_get("SPELL-USED") == player.zstate_get("W?FLOAT"):
        print("The " + parser.get_dobj().desc() + " sinks to the ground.")
        return
    elif player.zstate_get("SPELL-USED") == player.zstate_get("W?FUDGE"):
        print("The sweet smell has dispersed.")
        return
else:
    print("Nothing happens.")
    return
