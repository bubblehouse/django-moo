#!moo verb print-cont --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: PRINT-CONT
# params: OBJ, V?, LEVEL
# aux: Y, 1ST?, SHIT, AV, STR, PV?, INV?

obj = args[0] if len(args) > 0 else None
v_p = args[1] if len(args) > 1 else None
level = args[2] if len(args) > 2 else 0
y = None
v_1st_p = None
shit = None
av = None
str_v = None
pv_p = None
inv_p = None

if not (y := obj.contents.first()):
    return True
if (av := context.player.location) and _.zil_sdk.flag(av, "vehicle"):
    return True
else:
    av = None
v_1st_p = True
shit = True
if context.player in (obj, obj.location):
    inv_p = True
else:
    while True:
        if not y:
            break
        elif y == av:
            pv_p = True
        elif y == context.player:
            pass
        elif (
            not _.zil_sdk.flag(y, "invisible")
            and not _.zil_sdk.flag(y, "touchbit")
            and (str_v := y.get_property("first_description"))
        ):
            if not _.zil_sdk.flag(y, "obvious"):
                print(str_v)
                shit = None
            if (
                _.zork_thing.invoke_verb("see-inside?", y)
                and not y.location.get_property("descfcn")
                and y.contents.first()
            ):
                if _.zork_thing.invoke_verb("print-cont", y, v_p, 0):
                    v_1st_p = None
        y = _.zil_sdk.next_sibling(y)
y = obj.contents.first()
while True:
    if not y:
        if pv_p and av and av.contents.first():
            level = level + 1
            # ZIL: <PRINT-CONT ...>
            _.zork_thing.invoke_verb("print-cont", av, v_p, level)
        break
    elif y in (av, _.get_property("adventurer")):
        pass
    elif (
        not _.zil_sdk.flag(y, "invisible")
        and inv_p
        or _.zil_sdk.flag(y, "touchbit")
        or not y.get_property("first_description")
    ):
        if not _.zil_sdk.flag(y, "obvious"):
            if v_1st_p:
                if _.zork_thing.invoke_verb("firster", obj, level):
                    if level < 0:
                        level = 0
                level = 1 + level
                v_1st_p = None
            if level < 0:
                level = 0
            # ZIL: <DESCRIBE-OBJECT ...>
            _.zork_thing.invoke_verb("describe-object", y, v_p, level)
        elif y.contents.first() and _.zork_thing.invoke_verb("see-inside?", y):
            level = level + 1
            # ZIL: <PRINT-CONT ...>
            _.zork_thing.invoke_verb("print-cont", y, v_p, level)
            level = level - 1
    y = _.zil_sdk.next_sibling(y)
if v_1st_p and shit:
    return False
else:
    return True
