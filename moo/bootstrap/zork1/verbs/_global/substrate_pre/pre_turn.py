#!moo verb pre-turn --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: PRE-TURN

if _.zil_sdk.zstate_get("ZORK-NUMBER") == 3:
    if context.parser.get_iobj() == _.zil_sdk.zstate_get("ROOMS") and context.parser.get_dobj() in (
        _.zil_sdk.zstate_get("DIAL"),
        _.zil_sdk.zstate_get("TM-DIAL"),
        _.zil_sdk.zstate_get("T-BAR"),
    ):
        print("You should turn the " + _.zil_sdk.desc(context.parser.get_dobj()) + " to something.")
        return True
else:
    return True
if cond(
    [["==?", "ZORK-NUMBER", 1], ["AND", ["EQUAL?", "PRSI", "ROOMS"], ["NOT", ["EQUAL?", "PRSO", "BOOK"]]]],
    ["ELSE", ["EQUAL?", "PRSI", "ROOMS"]],
):
    return print("Your bare hands don't appear to be enough.")
elif not _.zil_sdk.flag(context.parser.get_dobj(), "turnable"):
    return print("You can't turn that!")
