#!moo verb attack break crack cut describe destroy disembark eat examine fight get give hit kill make mung pick slice smash stab take tell throw toss unboard what x --on $me --dspec either
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: CRETIN-FCN

if player_verb in ["tell"]:
    _.zil_sdk.zstate_set("P-CONT", None)
    _.zil_sdk.zstate_set("QUOTE-FLAG", None)
    return print("Talking to yourself is said to be a sign of impending mental collapse.")
elif player_verb in ["give"] and context.parser.get_iobj() == _.get_property("me"):
    _.zil_sdk.perform(_.zil_sdk.zstate_get("V?TAKE"), context.parser.get_dobj(), None)
    return True
elif player_verb in ["make"]:
    return print("Only you can do that.")
elif player_verb in ["disembark", "unboard"]:
    return print("You'll have to do that on your own.")
elif player_verb in ["eat"]:
    return print("Auto-cannibalism is not the answer.")
elif player_verb in [
    "attack",
    "kill",
    "hit",
    "fight",
    "stab",
    "cut",
    "slice",
    "mung",
    "destroy",
    "break",
    "smash",
    "crack",
]:
    if context.parser.get_iobj() and _.zil_sdk.flag(context.parser.get_iobj(), "weapon"):
        return _.zil_sdk.jigs_up("If you insist.... Poof, you're dead!")
    else:
        return print("Suicide is not the answer.")
elif player_verb in ["throw", "toss"]:
    if context.parser.get_dobj() == _.get_property("me"):
        return print("Why don't you just walk like normal people?")
elif player_verb in ["take", "get", "pick"]:
    return print("How romantic!")
elif player_verb in ["examine", "x", "describe", "what"]:
    if context.player.location in (_.get_property("mirror_1").location, _.get_property("mirror_2").location):
        return print("Your image in the mirror looks tired.")
    else:
        if _.zil_sdk.zstate_get("ZORK-NUMBER") == 3:
            return print("What you can see looks pretty much as usual, sorry to say.")
        else:
            return print("That's difficult unless your eyes are prehensile.")
# Auto-generated fallthrough to standard V-<player_verb>
_.zil_sdk.run_v_routine(player_verb)
