#!moo verb i-thief --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context, lookup
# ZIL routine: I-THIEF
# aux: RM, HERE?, ONCE, FLG

rm = lookup("thief").location
here_p = None
once = None
flg = None

if here_p := not _.zil_sdk.flag(lookup("thief"), "invisible"):
    rm = lookup("thief").location
if rm == _.get_property("treasure_room") and not rm == context.player.location:
    if here_p:
        # ZIL: <HACK-TREASURES ...>
        _.zork_thing.invoke_verb("hack-treasures")
        here_p = None
    # ZIL: <DEPOSIT-BOOTY ...>
    _.zork_thing.invoke_verb("deposit-booty", _.get_property("treasure_room"))
elif (
    rm == context.player.location
    and not _.zil_sdk.flag(rm, "onbit")
    and not lookup("troll").location == context.player.location
):
    if _.zork_thing.invoke_verb("thief-vs-adventurer", here_p):
        return True
    if _.zil_sdk.flag(lookup("thief"), "invisible"):
        here_p = None
else:
    if lookup("thief").location == rm and not _.zil_sdk.flag(lookup("thief"), "invisible"):
        _.zil_sdk.set_flag(lookup("thief"), "invisible", True)
        here_p = None
    if _.zil_sdk.flag(rm, "touchbit"):
        # ZIL: <ROB ...>
        _.zork_thing.invoke_verb("rob", rm, lookup("thief"), 75)
        flg = cond(
            [["AND", ["FSET?", ".RM", "MAZEBIT"], ["FSET?", "HERE", "MAZEBIT"]], ["ROB-MAZE", ".RM"]],
            ["T", ["STEAL-JUNK", ".RM"]],
        )
if (once := not once) and not here_p:
    # ZIL: <RECOVER-STILETTO ...>
    _.zork_thing.invoke_verb("recover-stiletto")
    while True:
        if rm and (rm := _.zil_sdk.next_sibling(rm)):
            pass
        else:
            rm = _.zil_sdk.zstate_get("ROOMS").contents.first()
        if not _.zil_sdk.flag(rm, "sacred") and _.zil_sdk.flag(rm, "outdoor"):
            _.zil_sdk.move(lookup("thief"), rm)
            _.zil_sdk.set_flag(lookup("thief"), "hostile", False)
            _.zil_sdk.set_flag(lookup("thief"), "invisible", True)
            _.zil_sdk.zstate_set("THIEF-HERE", None)
            break
    # ZIL: <AGAIN ...>
    lookup("again")
if not rm == _.get_property("treasure_room"):
    # ZIL: <DROP-JUNK ...>
    _.zork_thing.invoke_verb("drop-junk", rm)
return flg
