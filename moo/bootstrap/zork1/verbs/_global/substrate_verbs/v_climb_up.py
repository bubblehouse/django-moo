#!moo verb v-climb-up --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: V-CLIMB-UP
# params: DIR, OBJ
# aux: X, TX

dir = args[0] if len(args) > 0 else _.zil_sdk.zstate_get("P?UP")
obj = args[1] if len(args) > 1 else None
x = None
tx = None

if obj and not context.parser.get_dobj() == _.zil_sdk.zstate_get("ROOMS"):
    obj = context.parser.get_dobj()
if tx := getpt(context.player.location, dir):
    if obj:
        x = ptsize(tx)
        if (
            x == _.zil_sdk.zstate_get("NEXIT")
            or x in (_.zil_sdk.zstate_get("CEXIT"), _.zil_sdk.zstate_get("DEXIT"), _.zil_sdk.zstate_get("UEXIT"))
            and not _.zil_sdk.global_in(context.parser.get_dobj(), _.zil_sdk.table_get(tx, 0))
        ):
            print("The " + _.zil_sdk.desc(obj) + " do", end="")
            if not obj == _.get_property("stairs"):
                print("es", end="")
            print("n't lead ", end="")
            if dir == _.zil_sdk.zstate_get("P?UP"):
                print("up", end="")
            else:
                print("down", end="")
            print("ward.")
            return True
    _.zil_sdk.walk(dir)
    return True
elif obj and _.zork_thing.invoke_verb(
    "zmemq",
    _.zil_sdk.zstate_get("W?WALL"),
    (x := getpt(context.parser.get_dobj(), _.zil_sdk.zstate_get("P?SYNONYM"))),
    ptsize(x),
):
    return print("Climbing the walls is to no avail.")
elif cond(
    [
        ["==?", "ZORK-NUMBER", 1],
        ["AND", ["NOT", ["EQUAL?", "HERE", "PATH"]], ["EQUAL?", ".OBJ", "TREE"], ["GLOBAL-IN?", "TREE", "HERE"]],
    ],
    ["ELSE", ["NULL-F"]],
):
    print("There are no climbable trees here.")
    return True
elif obj == _.zil_sdk.zstate_get("ROOMS"):
    return print("You can't go that way.")
else:
    return print("You can't do that!")
