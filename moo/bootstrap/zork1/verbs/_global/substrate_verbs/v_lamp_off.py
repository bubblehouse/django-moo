#!moo verb v-lamp-off --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: V-LAMP-OFF

if _.zil_sdk.flag(context.parser.get_dobj(), "lit"):
    if not _.zil_sdk.flag(context.parser.get_dobj(), "onbit"):
        print("It is already off.")
    else:
        _.zil_sdk.set_flag(context.parser.get_dobj(), "onbit", False)
        if _.zil_sdk.zstate_get("LIT"):
            _.zil_sdk.zstate_set("LIT", _.zork_thing.invoke_verb("lit?", context.player.location))
        print("The " + _.zil_sdk.desc(context.parser.get_dobj()) + " is now off.")
        if not _.zil_sdk.zstate_get("LIT"):
            print("It is now pitch black.")
else:
    print("You can't turn that off.")
return True
