#!moo verb v-disenchant --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: V-DISENCHANT

if _.zil_sdk.zstate_get("ZORK-NUMBER") == 2:
    if not context.parser.get_dobj().location == context.player.location:
        return True
    elif (
        _.zil_sdk.zstate_get("SPELL-USED")
        in (_.zil_sdk.zstate_get("W?FEEBLE"), _.zil_sdk.zstate_get("W?FUMBLE"), _.zil_sdk.zstate_get("W?FEAR"))
        or _.zil_sdk.zstate_get("SPELL-USED")
        in (_.zil_sdk.zstate_get("W?FREEZE"), _.zil_sdk.zstate_get("W?FALL"), _.zil_sdk.zstate_get("W?FERMENT"))
        or _.zil_sdk.zstate_get("SPELL-USED")
        in (_.zil_sdk.zstate_get("W?FIERCE"), _.zil_sdk.zstate_get("W?FENCE"), _.zil_sdk.zstate_get("W?FANTASIZE"))
    ):
        if _.zil_sdk.flag(context.parser.get_dobj(), "actorbit"):
            if _.zil_sdk.zstate_get("SPELL-USED") == _.zil_sdk.zstate_get("W?FEEBLE"):
                return print("The " + _.zil_sdk.desc(context.parser.get_dobj()) + " seems stronger now.")
            elif _.zil_sdk.zstate_get("SPELL-USED") == _.zil_sdk.zstate_get("W?FUMBLE"):
                return print("The " + _.zil_sdk.desc(context.parser.get_dobj()) + " no longer appears clumsy.")
            elif _.zil_sdk.zstate_get("SPELL-USED") == _.zil_sdk.zstate_get("W?FEAR"):
                return print("The " + _.zil_sdk.desc(context.parser.get_dobj()) + " no longer appears afraid.")
            elif _.zil_sdk.zstate_get("SPELL-USED") == _.zil_sdk.zstate_get("W?FREEZE"):
                return print("The " + _.zil_sdk.desc(context.parser.get_dobj()) + " moves again.")
            elif _.zil_sdk.zstate_get("SPELL-USED") == _.zil_sdk.zstate_get("W?FERMENT"):
                return print("The " + _.zil_sdk.desc(context.parser.get_dobj()) + " stops swaying.")
            elif _.zil_sdk.zstate_get("SPELL-USED") == _.zil_sdk.zstate_get("W?FIERCE"):
                return print("The " + _.zil_sdk.desc(context.parser.get_dobj()) + " appears more peaceful.")
    elif _.zil_sdk.zstate_get("SPELL-USED") == _.zil_sdk.zstate_get("W?FLOAT"):
        return print("The " + _.zil_sdk.desc(context.parser.get_dobj()) + " sinks to the ground.")
    elif _.zil_sdk.zstate_get("SPELL-USED") == _.zil_sdk.zstate_get("W?FUDGE"):
        return print("The sweet smell has dispersed.")
else:
    return print("Nothing happens.")
