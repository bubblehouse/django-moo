#!moo verb v-dig --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: V-DIG

if not context.parser.get_iobj():
    return _.zil_sdk.zstate_set("PRSI", _.get_property("hands"))
if _.zil_sdk.zstate_get("ZORK-NUMBER") == 1:
    if context.parser.get_iobj() == _.get_property("shovel"):
        print("There's no reason to be digging here.")
        return True
else:
    return True
if _.zil_sdk.flag(context.parser.get_iobj(), "toolbit"):
    return print("Digging with the " + _.zil_sdk.desc(context.parser.get_iobj()) + " is slow and tedious.")
else:
    return print("Digging with a " + _.zil_sdk.desc(context.parser.get_iobj()) + " is silly.")
