#!moo verb v-pour-on --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: V-POUR-ON

if context.parser.get_dobj() == _.get_property("water"):
    _.zil_sdk.remove(context.parser.get_dobj())
    if flaming_p(context.parser.get_iobj()):
        print("The " + _.zil_sdk.desc(context.parser.get_iobj()) + " is extinguished.")
        if _.zil_sdk.zstate_get("ZORK-NUMBER") == 2:
            if context.parser.get_iobj() == _.zil_sdk.zstate_get("BINF-FLAG"):
                _.zil_sdk.zstate_set("BINF-FLAG", None)
        else:
            # ZIL: <NULL-F ...>
            _.zork_thing.invoke_verb("null-f")
        _.zil_sdk.set_flag(context.parser.get_iobj(), "onbit", False)
        return _.zil_sdk.set_flag(context.parser.get_iobj(), "flamebit", False)
    else:
        return print(
            "The water spills over the " + _.zil_sdk.desc(context.parser.get_iobj()) + ", to the floor, and evaporates."
        )
elif context.parser.get_dobj() == _.get_property("putty"):
    return _.zil_sdk.perform(_.zil_sdk.zstate_get("V?PUT"), _.get_property("putty"), context.parser.get_iobj())
else:
    return print("You can't pour that.")
