#!moo verb v-burn --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: V-BURN

if _.zork_thing.invoke_verb("null-f"):
    return False
elif _.zil_sdk.flag(context.parser.get_dobj(), "flammable"):
    if context.parser.get_dobj().location == context.player or context.player.location == context.parser.get_dobj():
        _.zil_sdk.remove(context.parser.get_dobj())
        print("The " + _.zil_sdk.desc(context.parser.get_dobj()), end="")
        print(" catches fire. Unfortunately, you were ", end="")
        if context.player.location == context.parser.get_dobj():
            print("in", end="")
        else:
            print("holding", end="")
        return _.zil_sdk.jigs_up(" it at the time.")
    else:
        _.zil_sdk.remove(context.parser.get_dobj())
        return print("The " + _.zil_sdk.desc(context.parser.get_dobj()) + " catches fire and is consumed.")
else:
    return print("You can't burn a " + _.zil_sdk.desc(context.parser.get_dobj()) + ".")
