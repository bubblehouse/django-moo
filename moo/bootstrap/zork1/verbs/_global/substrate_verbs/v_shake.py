#!moo verb v-shake --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: V-SHAKE

if _.zil_sdk.flag(context.parser.get_dobj(), "actorbit"):
    return print("This seems to have no effect.")
elif not _.zil_sdk.flag(context.parser.get_dobj(), "takeable"):
    return print("You can't take it; thus, you can't shake it!")
elif _.zil_sdk.flag(context.parser.get_dobj(), "contbit"):
    if _.zil_sdk.flag(context.parser.get_dobj(), "open"):
        if context.parser.get_dobj().contents.first():
            # ZIL: <SHAKE-LOOP ...>
            _.zork_thing.invoke_verb("shake-loop")
            print("The contents of the " + _.zil_sdk.desc(context.parser.get_dobj()) + " spill ", end="")
            if cond(
                [["==?", "ZORK-NUMBER", 3], ["FSET?", "HERE", "NONLANDBIT"]],
                ["ELSE", ["NOT", ["FSET?", "HERE", "RLANDBIT"]]],
            ):
                print("out and disappear", end="")
            else:
                print("to the ground", end="")
            return print(".")
        else:
            return print("Shaken.")
    else:
        if context.parser.get_dobj().contents.first():
            return print(
                "It sounds like there is something inside the " + _.zil_sdk.desc(context.parser.get_dobj()) + "."
            )
        else:
            return print("The " + _.zil_sdk.desc(context.parser.get_dobj()) + " sounds empty.")
else:
    return print("Shaken.")
