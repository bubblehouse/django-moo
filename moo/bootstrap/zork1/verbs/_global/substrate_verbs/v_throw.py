#!moo verb v-throw --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: V-THROW

if _.zork_thing.invoke_verb("idrop"):
    if context.parser.get_iobj() == _.get_property("me"):
        print("A terrific throw! The " + _.zil_sdk.desc(context.parser.get_dobj()), end="")
        _.zil_sdk.zstate_set("WINNER", context.player)
        return _.zil_sdk.jigs_up(
            " hits you squarely in the head. Normally,\nthis wouldn't do much damage, but by incredible mischance, you fall over\nbackwards trying to duck, and break your neck, justice being swift and\nmerciful in the Great Underground Empire."
        )
    elif context.parser.get_iobj() and _.zil_sdk.flag(context.parser.get_iobj(), "actorbit"):
        return print(
            "The "
            + _.zil_sdk.desc(context.parser.get_iobj())
            + " ducks as the "
            + _.zil_sdk.desc(context.parser.get_dobj())
            + " flies by and crashes to the ground."
        )
    else:
        return print("Thrown.")
else:
    return print("Huh?")
