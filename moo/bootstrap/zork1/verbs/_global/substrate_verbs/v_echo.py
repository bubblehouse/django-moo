#!moo verb v-echo --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
# ZIL routine: V-ECHO
# aux: LST, MAX, ECH, CNT

lst = None
max_v = None
ech = 0
cnt = None

_.zil_sdk.zstate_get("#DECL")
if _.zil_sdk.zstate_get("ZORK-NUMBER") == 1:
    if _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-LEXV"), _.zil_sdk.zstate_get("P-LEXWORDS")) > 0:
        lst = rest(
            _.zil_sdk.zstate_get("P-LEXV"),
            _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-LEXV"), _.zil_sdk.zstate_get("P-LEXWORDS"))
            * _.zil_sdk.zstate_get("P-WORDLEN"),
        )
        max_v = _.zil_sdk.table_get(lst, 0) + _.zil_sdk.table_get(lst, 1) - 1
        while True:
            if (ech := ech + 1) > 2:
                print("...")
                break
            else:
                cnt = _.zil_sdk.table_get(lst, 1) - 1
                while True:
                    if (cnt := cnt + 1) > max_v:
                        break
                    else:
                        print(chr(_.zil_sdk.table_get(_.zil_sdk.zstate_get("P-INBUF"), cnt)), end="")
                return print(" ", end="")
    else:
        return print("echo echo ...")
else:
    return print("echo echo ...")
