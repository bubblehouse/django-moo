#!moo verb v-find --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: V-FIND
# aux: L

l = context.parser.get_dobj().location

if context.parser.get_dobj() in (_.get_property("hands"), _.get_property("lungs")):
    return print("Within six feet of your head, assuming you haven't left that\nsomewhere.")
elif context.parser.get_dobj() == _.get_property("me"):
    return print("You're around here somewhere...")
elif l == _.get_property("global_objects"):
    return print("You find it.")
elif context.parser.get_dobj().location == context.player:
    return print("You have it.")
elif (
    context.parser.get_dobj().location == context.player.location
    or _.zil_sdk.global_in(context.parser.get_dobj(), context.player.location)
    or context.parser.get_dobj() == _.get_property("pseudo_object")
):
    return print("It's right here.")
elif _.zil_sdk.flag(l, "actorbit"):
    return print("The " + _.zil_sdk.desc(l) + " has it.")
elif _.zil_sdk.flag(l, "surfacebit"):
    return print("It's on the " + _.zil_sdk.desc(l) + ".")
elif _.zil_sdk.flag(l, "contbit"):
    return print("It's in the " + _.zil_sdk.desc(l) + ".")
else:
    return print("Beats me.")
