#!moo verb v-lamp-on --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: V-LAMP-ON

if _.zil_sdk.flag(context.parser.get_dobj(), "lit"):
    if _.zil_sdk.flag(context.parser.get_dobj(), "onbit"):
        print("It is already on.")
    else:
        _.zil_sdk.set_flag(context.parser.get_dobj(), "onbit", True)
        print("The " + _.zil_sdk.desc(context.parser.get_dobj()) + " is now on.")
        if not _.zil_sdk.zstate_get("LIT"):
            _.zil_sdk.zstate_set("LIT", _.zork_thing.invoke_verb("lit?", context.player.location))
            print()
            # ZIL: <V-LOOK ...>
            _.zork_thing.invoke_verb("v-look")
elif _.zil_sdk.flag(context.parser.get_dobj(), "flammable"):
    print("If you wish to burn the " + _.zil_sdk.desc(context.parser.get_dobj()) + ", you should say so.")
else:
    print("You can't turn that on.")
return True
