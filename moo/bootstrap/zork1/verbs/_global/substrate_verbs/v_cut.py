#!moo verb v-cut --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: V-CUT

if _.zil_sdk.flag(context.parser.get_dobj(), "actorbit"):
    return _.zil_sdk.perform(_.zil_sdk.zstate_get("V?ATTACK"), context.parser.get_dobj(), context.parser.get_iobj())
elif _.zil_sdk.flag(context.parser.get_dobj(), "flammable") and _.zil_sdk.flag(context.parser.get_iobj(), "weapon"):
    if context.player.location == context.parser.get_dobj():
        print("Not a bright idea, especially since you're in it.")
        return True
    _.zil_sdk.remove(context.parser.get_dobj())
    return print(
        "Your skillful "
        + _.zil_sdk.desc(context.parser.get_iobj())
        + "smanship slices the "
        + _.zil_sdk.desc(context.parser.get_dobj())
        + " into innumerable slivers which blow away."
    )
elif not _.zil_sdk.flag(context.parser.get_iobj(), "weapon"):
    return print('The "cutting edge" of a ' + _.zil_sdk.desc(context.parser.get_iobj()) + " is hardly adequate.")
else:
    return print("Strange concept, cutting the " + _.zil_sdk.desc(context.parser.get_dobj()) + "....")
