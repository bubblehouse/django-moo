#!moo verb parser --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: PARSER
# aux: PTR, WRD, VAL, VERB, OF-FLAG, OWINNER, OMERGED, LEN, DIR, NW, LW, CNT

ptr = _.zil_sdk.zstate_get("P-LEXSTART")
wrd = None
val = 0
verb = None
of_flag = None
owinner = None
omerged = None
len_v = None
dir = None
nw = 0
lw = 0
cnt = -1

while True:
    if (cnt := cnt + 1) > _.zil_sdk.zstate_get("P-ITBLLEN"):
        break
    else:
        if not _.zil_sdk.zstate_get("P-OFLAG"):
            # ZIL: <PUT ...>
            _.zil_sdk.table_put(
                _.zil_sdk.zstate_get("P-OTBL"), cnt, _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-ITBL"), cnt)
            )
        # ZIL: <PUT ...>
        return _.zil_sdk.table_put(_.zil_sdk.zstate_get("P-ITBL"), cnt, 0)
owinner = context.player
omerged = _.zil_sdk.zstate_get("P-MERGED")
_.zil_sdk.zstate_set("P-ADVERB", None)
_.zil_sdk.zstate_set("P-MERGED", None)
_.zil_sdk.zstate_set("P-END-ON-PREP", None)
# ZIL: <PUT ...>
_.zil_sdk.table_put(_.zil_sdk.zstate_get("P-PRSO"), _.zil_sdk.zstate_get("P-MATCHLEN"), 0)
# ZIL: <PUT ...>
_.zil_sdk.table_put(_.zil_sdk.zstate_get("P-PRSI"), _.zil_sdk.zstate_get("P-MATCHLEN"), 0)
# ZIL: <PUT ...>
_.zil_sdk.table_put(_.zil_sdk.zstate_get("P-BUTS"), _.zil_sdk.zstate_get("P-MATCHLEN"), 0)
if not _.zil_sdk.zstate_get("QUOTE-FLAG") and context.player != context.player:
    _.zil_sdk.zstate_set("WINNER", context.player)
    _.zil_sdk.zstate_set("HERE", _.zork_thing.invoke_verb("meta-loc", context.player))
    return _.zil_sdk.zstate_set("LIT", _.zork_thing.invoke_verb("lit?", context.player.location))
if _.zil_sdk.zstate_get("RESERVE-PTR"):
    ptr = _.zil_sdk.zstate_get("RESERVE-PTR")
    # ZIL: <STUFF ...>
    _.zork_thing.invoke_verb("stuff", _.zil_sdk.zstate_get("RESERVE-LEXV"), _.zil_sdk.zstate_get("P-LEXV"))
    if not _.zil_sdk.zstate_get("SUPER-BRIEF") and context.player == context.player:
        print()
    _.zil_sdk.zstate_set("RESERVE-PTR", None)
    return _.zil_sdk.zstate_set("P-CONT", None)
elif context.player:
    ptr = context.player
    if not _.zil_sdk.zstate_get("SUPER-BRIEF") and context.player == context.player and not player_verb in ["say"]:
        print()
    return _.zil_sdk.zstate_set("P-CONT", None)
else:
    _.zil_sdk.zstate_set("WINNER", context.player)
    _.zil_sdk.zstate_set("QUOTE-FLAG", None)
    if not _.zil_sdk.flag(context.player.location, "vehicle"):
        _.zil_sdk.zstate_set("HERE", context.player.location)
    _.zil_sdk.zstate_set("LIT", _.zork_thing.invoke_verb("lit?", context.player.location))
    if not _.zil_sdk.zstate_get("SUPER-BRIEF"):
        print()
    print(">", end="")
    # ZIL: <READ ...>
    return read(_.zil_sdk.zstate_get("P-INBUF"), _.zil_sdk.zstate_get("P-LEXV"))
_.zil_sdk.zstate_set("P-LEN", _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-LEXV"), _.zil_sdk.zstate_get("P-LEXWORDS")))
if _.zil_sdk.zstate_get("P-LEN") == 0:
    print("I beg your pardon?")
    return False
if (wrd := _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-LEXV"), ptr)) == _.zil_sdk.zstate_get("W?OOPS"):
    if _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-LEXV"), ptr + _.zil_sdk.zstate_get("P-LEXELEN")) in (
        _.zil_sdk.zstate_get("W?PERIOD"),
        _.zil_sdk.zstate_get("W?COMMA"),
    ):
        ptr = ptr + _.zil_sdk.zstate_get("P-LEXELEN")
        return _.zil_sdk.zstate_set("P-LEN", _.zil_sdk.zstate_get("P-LEN") - 1)
    if not _.zil_sdk.zstate_get("P-LEN") > 1:
        print("I can't help your clumsiness.")
        return False
    elif _.zil_sdk.table_get(_.zil_sdk.zstate_get("OOPS-TABLE"), _.zil_sdk.zstate_get("O-PTR")):
        if _.zil_sdk.zstate_get("P-LEN") > 2 and _.zil_sdk.table_get(
            _.zil_sdk.zstate_get("P-LEXV"), ptr + _.zil_sdk.zstate_get("P-LEXELEN")
        ) == _.zil_sdk.zstate_get("W?QUOTE"):
            print("Sorry, you can't correct mistakes in quoted text.")
            return False
        elif _.zil_sdk.zstate_get("P-LEN") > 2:
            print("Warning: only the first word after OOPS is used.")
        # ZIL: <PUT ...>
        _.zil_sdk.table_put(
            _.zil_sdk.zstate_get("AGAIN-LEXV"),
            _.zil_sdk.table_get(_.zil_sdk.zstate_get("OOPS-TABLE"), _.zil_sdk.zstate_get("O-PTR")),
            _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-LEXV"), ptr + _.zil_sdk.zstate_get("P-LEXELEN")),
        )
        _.zil_sdk.zstate_set("WINNER", owinner)
        # ZIL: <INBUF-ADD ...>
        _.zork_thing.invoke_verb(
            "inbuf-add",
            _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-LEXV"), ptr * _.zil_sdk.zstate_get("P-LEXELEN") + 6),
            _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-LEXV"), ptr * _.zil_sdk.zstate_get("P-LEXELEN") + 7),
            _.zil_sdk.table_get(_.zil_sdk.zstate_get("OOPS-TABLE"), _.zil_sdk.zstate_get("O-PTR"))
            * _.zil_sdk.zstate_get("P-LEXELEN")
            + 3,
        )
        # ZIL: <STUFF ...>
        _.zork_thing.invoke_verb("stuff", _.zil_sdk.zstate_get("AGAIN-LEXV"), _.zil_sdk.zstate_get("P-LEXV"))
        _.zil_sdk.zstate_set(
            "P-LEN", _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-LEXV"), _.zil_sdk.zstate_get("P-LEXWORDS"))
        )
        ptr = _.zil_sdk.table_get(_.zil_sdk.zstate_get("OOPS-TABLE"), _.zil_sdk.zstate_get("O-START"))
        # ZIL: <INBUF-STUFF ...>
        return _.zork_thing.invoke_verb(
            "inbuf-stuff", _.zil_sdk.zstate_get("OOPS-INBUF"), _.zil_sdk.zstate_get("P-INBUF")
        )
    else:
        # ZIL: <PUT ...>
        _.zil_sdk.table_put(_.zil_sdk.zstate_get("OOPS-TABLE"), _.zil_sdk.zstate_get("O-END"), None)
        print("There was no word to replace!")
        return False
else:
    if not wrd in (_.zil_sdk.zstate_get("W?AGAIN"), _.zil_sdk.zstate_get("W?G")):
        _.zil_sdk.zstate_set("P-NUMBER", 0)
    # ZIL: <PUT ...>
    return _.zil_sdk.table_put(_.zil_sdk.zstate_get("OOPS-TABLE"), _.zil_sdk.zstate_get("O-END"), None)
if _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-LEXV"), ptr) in (
    _.zil_sdk.zstate_get("W?AGAIN"),
    _.zil_sdk.zstate_get("W?G"),
):
    if _.zil_sdk.table_get(_.zil_sdk.zstate_get("OOPS-INBUF"), 1) == 0:
        print("Beg pardon?")
        return False
    elif _.zil_sdk.zstate_get("P-OFLAG"):
        print("It's difficult to repeat fragments.")
        return False
    elif not _.zil_sdk.zstate_get("P-WON"):
        print("That would just repeat a mistake.")
        return False
    elif _.zil_sdk.zstate_get("P-LEN") > 1:
        if _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-LEXV"), ptr + _.zil_sdk.zstate_get("P-LEXELEN")) in (
            _.zil_sdk.zstate_get("W?PERIOD"),
            _.zil_sdk.zstate_get("W?COMMA"),
            _.zil_sdk.zstate_get("W?THEN"),
        ) or _.zil_sdk.table_get(
            _.zil_sdk.zstate_get("P-LEXV"), ptr + _.zil_sdk.zstate_get("P-LEXELEN")
        ) == _.zil_sdk.zstate_get("W?AND"):
            ptr = ptr + 2 * _.zil_sdk.zstate_get("P-LEXELEN")
            # ZIL: <PUTB ...>
            return putb(
                _.zil_sdk.zstate_get("P-LEXV"),
                _.zil_sdk.zstate_get("P-LEXWORDS"),
                _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-LEXV"), _.zil_sdk.zstate_get("P-LEXWORDS")) - 2,
            )
        else:
            print("I couldn't understand that sentence.")
            return False
    else:
        ptr = ptr + _.zil_sdk.zstate_get("P-LEXELEN")
        # ZIL: <PUTB ...>
        return putb(
            _.zil_sdk.zstate_get("P-LEXV"),
            _.zil_sdk.zstate_get("P-LEXWORDS"),
            _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-LEXV"), _.zil_sdk.zstate_get("P-LEXWORDS")) - 1,
        )
    if _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-LEXV"), _.zil_sdk.zstate_get("P-LEXWORDS")) > 0:
        # ZIL: <STUFF ...>
        _.zork_thing.invoke_verb("stuff", _.zil_sdk.zstate_get("P-LEXV"), _.zil_sdk.zstate_get("RESERVE-LEXV"))
        return _.zil_sdk.zstate_set("RESERVE-PTR", ptr)
    else:
        return _.zil_sdk.zstate_set("RESERVE-PTR", None)
    _.zil_sdk.zstate_set("WINNER", owinner)
    _.zil_sdk.zstate_set("P-MERGED", omerged)
    # ZIL: <INBUF-STUFF ...>
    _.zork_thing.invoke_verb("inbuf-stuff", _.zil_sdk.zstate_get("OOPS-INBUF"), _.zil_sdk.zstate_get("P-INBUF"))
    # ZIL: <STUFF ...>
    _.zork_thing.invoke_verb("stuff", _.zil_sdk.zstate_get("AGAIN-LEXV"), _.zil_sdk.zstate_get("P-LEXV"))
    cnt = -1
    dir = _.zil_sdk.zstate_get("AGAIN-DIR")
    while True:
        if igrtr_p(cnt, _.zil_sdk.zstate_get("P-ITBLLEN")):
            break
        else:
            # ZIL: <PUT ...>
            return _.zil_sdk.table_put(
                _.zil_sdk.zstate_get("P-ITBL"), cnt, _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-OTBL"), cnt)
            )
else:
    # ZIL: <STUFF ...>
    _.zork_thing.invoke_verb("stuff", _.zil_sdk.zstate_get("P-LEXV"), _.zil_sdk.zstate_get("AGAIN-LEXV"))
    # ZIL: <INBUF-STUFF ...>
    _.zork_thing.invoke_verb("inbuf-stuff", _.zil_sdk.zstate_get("P-INBUF"), _.zil_sdk.zstate_get("OOPS-INBUF"))
    # ZIL: <PUT ...>
    _.zil_sdk.table_put(_.zil_sdk.zstate_get("OOPS-TABLE"), _.zil_sdk.zstate_get("O-START"), ptr)
    # ZIL: <PUT ...>
    _.zil_sdk.table_put(
        _.zil_sdk.zstate_get("OOPS-TABLE"), _.zil_sdk.zstate_get("O-LENGTH"), 4 * _.zil_sdk.zstate_get("P-LEN")
    )
    len_v = 2 * ptr + _.zil_sdk.zstate_get("P-LEXELEN") * _.zil_sdk.table_get(
        _.zil_sdk.zstate_get("P-LEXV"), _.zil_sdk.zstate_get("P-LEXWORDS")
    )
    # ZIL: <PUT ...>
    _.zil_sdk.table_put(
        _.zil_sdk.zstate_get("OOPS-TABLE"),
        _.zil_sdk.zstate_get("O-END"),
        _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-LEXV"), len_v - 1)
        + _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-LEXV"), len_v - 2),
    )
    _.zil_sdk.zstate_set("RESERVE-PTR", None)
    len_v = _.zil_sdk.zstate_get("P-LEN")
    _.zil_sdk.zstate_set("P-DIR", None)
    _.zil_sdk.zstate_set("P-NCN", 0)
    _.zil_sdk.zstate_set("P-GETFLAGS", 0)
    while True:
        if _.zil_sdk.zstate_set("P-LEN", _.zil_sdk.zstate_get("P-LEN") - 1) < 0:
            _.zil_sdk.zstate_set("QUOTE-FLAG", None)
            break
        elif (wrd := _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-LEXV"), ptr)) or (
            wrd := _.zork_thing.invoke_verb("number?", ptr)
        ):
            if _.zil_sdk.zstate_get("P-LEN") == 0:
                nw = 0
            else:
                nw = _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-LEXV"), ptr + _.zil_sdk.zstate_get("P-LEXELEN"))
            if wrd == _.zil_sdk.zstate_get("W?TO") and verb == _.zil_sdk.zstate_get("ACT?TELL"):
                wrd = _.zil_sdk.zstate_get("W?QUOTE")
            elif (
                wrd == _.zil_sdk.zstate_get("W?THEN")
                and _.zil_sdk.zstate_get("P-LEN") > 0
                and not verb
                and not _.zil_sdk.zstate_get("QUOTE-FLAG")
            ):
                if lw in (0, _.zil_sdk.zstate_get("W?PERIOD")):
                    wrd = _.zil_sdk.zstate_get("W?THE")
                else:
                    # ZIL: <PUT ...>
                    _.zil_sdk.table_put(
                        _.zil_sdk.zstate_get("P-ITBL"), _.zil_sdk.zstate_get("P-VERB"), _.zil_sdk.zstate_get("ACT?TELL")
                    )
                    # ZIL: <PUT ...>
                    _.zil_sdk.table_put(_.zil_sdk.zstate_get("P-ITBL"), _.zil_sdk.zstate_get("P-VERBN"), 0)
                    wrd = _.zil_sdk.zstate_get("W?QUOTE")
            if wrd in (
                _.zil_sdk.zstate_get("W?THEN"),
                _.zil_sdk.zstate_get("W?PERIOD"),
                _.zil_sdk.zstate_get("W?QUOTE"),
            ):
                if wrd == _.zil_sdk.zstate_get("W?QUOTE"):
                    if _.zil_sdk.zstate_get("QUOTE-FLAG"):
                        _.zil_sdk.zstate_set("QUOTE-FLAG", None)
                    else:
                        _.zil_sdk.zstate_set("QUOTE-FLAG", True)
                if not _.zil_sdk.zstate_get("P-LEN") == 0:
                    _.zil_sdk.zstate_set("P-CONT", ptr + _.zil_sdk.zstate_get("P-LEXELEN"))
                # ZIL: <PUTB ...>
                putb(_.zil_sdk.zstate_get("P-LEXV"), _.zil_sdk.zstate_get("P-LEXWORDS"), _.zil_sdk.zstate_get("P-LEN"))
                break
            elif (
                (
                    val := _.zork_thing.invoke_verb(
                        "wt?", wrd, _.zil_sdk.zstate_get("PS?DIRECTION"), _.zil_sdk.zstate_get("P1?DIRECTION")
                    )
                )
                and verb == _.zil_sdk.zstate_get("ACT?WALK")
                and len_v == 1
                or len_v == 2
                and verb == _.zil_sdk.zstate_get("ACT?WALK")
                or nw
                in (_.zil_sdk.zstate_get("W?THEN"), _.zil_sdk.zstate_get("W?PERIOD"), _.zil_sdk.zstate_get("W?QUOTE"))
                and not len_v < 2
                or _.zil_sdk.zstate_get("QUOTE-FLAG")
                and len_v == 2
                and nw == _.zil_sdk.zstate_get("W?QUOTE")
                or len_v > 2
                and nw in (_.zil_sdk.zstate_get("W?COMMA"), _.zil_sdk.zstate_get("W?AND"))
            ):
                dir = val
                if nw in (_.zil_sdk.zstate_get("W?COMMA"), _.zil_sdk.zstate_get("W?AND")):
                    # ZIL: <PUT ...>
                    _.zil_sdk.table_put(
                        _.zil_sdk.zstate_get("P-LEXV"),
                        ptr + _.zil_sdk.zstate_get("P-LEXELEN"),
                        _.zil_sdk.zstate_get("W?THEN"),
                    )
                if not len_v > 2:
                    _.zil_sdk.zstate_set("QUOTE-FLAG", None)
                    break
            elif (
                val := _.zork_thing.invoke_verb(
                    "wt?", wrd, _.zil_sdk.zstate_get("PS?VERB"), _.zil_sdk.zstate_get("P1?VERB")
                )
            ) and not verb:
                verb = val
                # ZIL: <PUT ...>
                _.zil_sdk.table_put(_.zil_sdk.zstate_get("P-ITBL"), _.zil_sdk.zstate_get("P-VERB"), val)
                # ZIL: <PUT ...>
                _.zil_sdk.table_put(
                    _.zil_sdk.zstate_get("P-ITBL"), _.zil_sdk.zstate_get("P-VERBN"), _.zil_sdk.zstate_get("P-VTBL")
                )
                # ZIL: <PUT ...>
                _.zil_sdk.table_put(_.zil_sdk.zstate_get("P-VTBL"), 0, wrd)
                # ZIL: <PUTB ...>
                putb(
                    _.zil_sdk.zstate_get("P-VTBL"),
                    2,
                    _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-LEXV"), (cnt := ptr * 2 + 2)),
                )
                # ZIL: <PUTB ...>
                putb(_.zil_sdk.zstate_get("P-VTBL"), 3, _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-LEXV"), cnt + 1))
            elif (
                (val := _.zork_thing.invoke_verb("wt?", wrd, _.zil_sdk.zstate_get("PS?PREPOSITION"), 0))
                or wrd in (_.zil_sdk.zstate_get("W?ALL"), _.zil_sdk.zstate_get("W?ONE"))
                or _.zork_thing.invoke_verb("wt?", wrd, _.zil_sdk.zstate_get("PS?ADJECTIVE"))
                or _.zork_thing.invoke_verb("wt?", wrd, _.zil_sdk.zstate_get("PS?OBJECT"))
            ):
                if (
                    _.zil_sdk.zstate_get("P-LEN") > 1
                    and nw == _.zil_sdk.zstate_get("W?OF")
                    and val == 0
                    and not wrd
                    in (_.zil_sdk.zstate_get("W?ALL"), _.zil_sdk.zstate_get("W?ONE"), _.zil_sdk.zstate_get("W?A"))
                ):
                    of_flag = True
                elif (
                    not val == 0
                    and _.zil_sdk.zstate_get("P-LEN") == 0
                    or nw in (_.zil_sdk.zstate_get("W?THEN"), _.zil_sdk.zstate_get("W?PERIOD"))
                ):
                    _.zil_sdk.zstate_set("P-END-ON-PREP", True)
                    if _.zil_sdk.zstate_get("P-NCN") < 2:
                        # ZIL: <PUT ...>
                        _.zil_sdk.table_put(_.zil_sdk.zstate_get("P-ITBL"), _.zil_sdk.zstate_get("P-PREP1"), val)
                        # ZIL: <PUT ...>
                        _.zil_sdk.table_put(_.zil_sdk.zstate_get("P-ITBL"), _.zil_sdk.zstate_get("P-PREP1N"), wrd)
                elif _.zil_sdk.zstate_get("P-NCN") == 2:
                    print("There were too many nouns in that sentence.")
                    return False
                else:
                    _.zil_sdk.zstate_set("P-NCN", _.zil_sdk.zstate_get("P-NCN") + 1)
                    _.zil_sdk.zstate_set("P-ACT", verb)
                    if not (ptr := _.zork_thing.invoke_verb("clause", ptr, val, wrd)):
                        return False
                    if ptr < 0:
                        _.zil_sdk.zstate_set("QUOTE-FLAG", None)
                        break
            elif wrd == _.zil_sdk.zstate_get("W?OF"):
                if not of_flag or nw in (_.zil_sdk.zstate_get("W?PERIOD"), _.zil_sdk.zstate_get("W?THEN")):
                    # ZIL: <CANT-USE ...>
                    _.zork_thing.invoke_verb("cant-use", ptr)
                    return False
                else:
                    of_flag = None
            elif _.zork_thing.invoke_verb("wt?", wrd, _.zil_sdk.zstate_get("PS?BUZZ-WORD")):
                pass
            elif (
                verb == _.zil_sdk.zstate_get("ACT?TELL")
                and _.zork_thing.invoke_verb(
                    "wt?", wrd, _.zil_sdk.zstate_get("PS?VERB"), _.zil_sdk.zstate_get("P1?VERB")
                )
                and context.player == context.player
            ):
                print("Please consult your manual for the correct way to talk to other people\nor creatures.")
                return False
            else:
                # ZIL: <CANT-USE ...>
                _.zork_thing.invoke_verb("cant-use", ptr)
                return False
        else:
            # ZIL: <UNKNOWN-WORD ...>
            _.zork_thing.invoke_verb("unknown-word", ptr)
            return False
        lw = wrd
        ptr = ptr + _.zil_sdk.zstate_get("P-LEXELEN")
# ZIL: <PUT ...>
_.zil_sdk.table_put(_.zil_sdk.zstate_get("OOPS-TABLE"), _.zil_sdk.zstate_get("O-PTR"), None)
if dir:
    _.zil_sdk.zstate_set("PRSA", _.zil_sdk.zstate_get("V?WALK"))
    _.zil_sdk.zstate_set("PRSO", dir)
    _.zil_sdk.zstate_set("P-OFLAG", None)
    _.zil_sdk.zstate_set("P-WALK-DIR", dir)
    return _.zil_sdk.zstate_set("AGAIN-DIR", dir)
else:
    if _.zil_sdk.zstate_get("P-OFLAG"):
        # ZIL: <ORPHAN-MERGE ...>
        return _.zork_thing.invoke_verb("orphan-merge")
    _.zil_sdk.zstate_set("P-WALK-DIR", None)
    _.zil_sdk.zstate_set("AGAIN-DIR", None)
    if (
        _.zork_thing.invoke_verb("syntax-check")
        and _.zork_thing.invoke_verb("snarf-objects")
        and _.zork_thing.invoke_verb("many-check")
        and _.zork_thing.invoke_verb("take-check")
    ):
        return True
