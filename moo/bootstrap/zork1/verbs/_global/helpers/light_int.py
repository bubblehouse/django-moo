#!moo verb light-int --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: LIGHT-INT
# params: OBJ, TBL, TICK

obj = args[0] if len(args) > 0 else None
tbl = args[1] if len(args) > 1 else None
tick = args[2] if len(args) > 2 else None

if tick == 0:
    _.zil_sdk.set_flag(obj, "onbit", False)
    return _.zil_sdk.set_flag(obj, "rmungbit", True)
if _.zork_thing.invoke_verb("held?", obj) or obj.location == context.player.location:
    if tick == 0:
        return print("You'd better have more light than from the " + _.zil_sdk.desc(obj) + ".")
    else:
        return print(_.zil_sdk.table_get(tbl, 1))
