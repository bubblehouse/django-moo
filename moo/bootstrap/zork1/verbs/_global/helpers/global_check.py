#!moo verb global-check --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: GLOBAL-CHECK
# params: TBL
# aux: LEN, RMG, RMGL, CNT, OBJ, OBITS, FOO

tbl = args[0] if len(args) > 0 else None
len_v = None
rmg = None
rmgl = None
cnt = 0
obj = None
obits = None
foo = None

len_v = _.zil_sdk.table_get(tbl, _.zil_sdk.zstate_get("P-MATCHLEN"))
obits = _.zil_sdk.zstate_get("P-SLOCBITS")
if rmg := getpt(context.player.location, _.zil_sdk.zstate_get("P?GLOBAL")):
    rmgl = ptsize(rmg) - 1
    while True:
        if _.zork_thing.invoke_verb("this-it?", (obj := _.zil_sdk.table_get(rmg, cnt)), tbl):
            # ZIL: <OBJ-FOUND ...>
            return _.zork_thing.invoke_verb("obj-found", obj, tbl)
        if igrtr_p(cnt, rmgl):
            break
if rmg := getpt(context.player.location, _.zil_sdk.zstate_get("P?PSEUDO")):
    rmgl = [["PTSIZE", ".RMG"], 4] - 1
    cnt = 0
    while True:
        if _.zil_sdk.zstate_get("P-NAM") == _.zil_sdk.table_get(rmg, cnt * 2):
            _.get_property("pseudo_object").set_property("action", _.zil_sdk.table_get(rmg, cnt * 2 + 1))
            foo = back(getpt(_.get_property("pseudo_object"), _.zil_sdk.zstate_get("P?ACTION")), 5)
            # ZIL: <PUT ...>
            _.zil_sdk.table_put(foo, 0, _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-NAM"), 0))
            # ZIL: <PUT ...>
            _.zil_sdk.table_put(foo, 1, _.zil_sdk.table_get(_.zil_sdk.zstate_get("P-NAM"), 1))
            # ZIL: <OBJ-FOUND ...>
            _.zork_thing.invoke_verb("obj-found", _.get_property("pseudo_object"), tbl)
            break
        elif igrtr_p(cnt, rmgl):
            break
if _.zil_sdk.table_get(tbl, _.zil_sdk.zstate_get("P-MATCHLEN")) == len_v:
    _.zil_sdk.zstate_set("P-SLOCBITS", -1)
    _.zil_sdk.zstate_set("P-TABLE", tbl)
    # ZIL: <DO-SL ...>
    _.zork_thing.invoke_verb("do-sl", _.get_property("global_objects"), 1, 1)
    _.zil_sdk.zstate_set("P-SLOCBITS", obits)
    if _.zil_sdk.table_get(tbl, _.zil_sdk.zstate_get("P-MATCHLEN")) == 0 and verb_name in (
        _.zil_sdk.zstate_get("V?LOOK-INSIDE"),
        _.zil_sdk.zstate_get("V?SEARCH"),
        _.zil_sdk.zstate_get("V?EXAMINE"),
    ):
        # ZIL: <DO-SL ...>
        return _.zork_thing.invoke_verb("do-sl", _.zil_sdk.zstate_get("ROOMS"), 1, 1)
