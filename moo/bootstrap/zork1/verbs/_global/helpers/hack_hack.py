#!moo verb hack-hack --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: HACK-HACK
# params: STR

str_v = args[0] if len(args) > 0 else None

if context.parser.get_dobj().location == _.get_property("global_objects") and player_verb in ["wave", "raise", "lower"]:
    return print("The " + _.zil_sdk.desc(context.parser.get_dobj()) + " isn't here!")
else:
    return print(str_v + _.zil_sdk.desc(context.parser.get_dobj()) + _.zil_sdk.pick(_.zil_sdk.zstate_get("HO-HUM")))
