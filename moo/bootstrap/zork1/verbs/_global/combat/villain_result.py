#!moo verb villain-result --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
# ZIL routine: VILLAIN-RESULT
# params: VILLAIN, DEF, RES

villain = args[0] if len(args) > 0 else None
def_v = args[1] if len(args) > 1 else None
res = args[2] if len(args) > 2 else None

villain.set_property("strength", def_v)
if def_v == 0:
    _.zil_sdk.set_flag(villain, "hostile", False)
    print(
        "Almost as soon as the "
        + _.zil_sdk.desc(villain)
        + " breathes his last breath, a cloud\nof sinister black fog envelops him, and when the fog lifts, the\ncarcass has disappeared."
    )
    _.zil_sdk.remove(villain)
    # ZIL: <APPLY ...>
    apply(_.zil_sdk.getp(villain, "action"), _.zil_sdk.zstate_get("F-DEAD"))
    return res
elif res == _.zil_sdk.zstate_get("UNCONSCIOUS"):
    # ZIL: <APPLY ...>
    apply(_.zil_sdk.getp(villain, "action"), _.zil_sdk.zstate_get("F-UNCONSCIOUS"))
    return res
else:
    return res
