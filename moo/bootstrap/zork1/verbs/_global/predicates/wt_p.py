#!moo verb wt? --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
# ZIL routine: WT?
# params: PTR, BIT, B1
# aux: OFFS, TYP

ptr = args[0] if len(args) > 0 else None
bit = args[1] if len(args) > 1 else None
b1 = args[2] if len(args) > 2 else 5
offs = _.zil_sdk.zstate_get("P-P1OFF")
typ = None

if btst((typ := _.zil_sdk.table_get(ptr, _.zil_sdk.zstate_get("P-PSOFF"))), bit):
    if b1 > 4:
        return True
    else:
        typ = band(typ, _.zil_sdk.zstate_get("P-P1BITS"))
        if not typ == b1:
            offs = offs + 1
        # ZIL: <GETB ...>
        return _.zil_sdk.table_get(ptr, offs)
