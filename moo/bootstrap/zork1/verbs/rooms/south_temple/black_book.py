#!moo verb burn close light open read-page rotate shut turn --on $book --dspec either
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: BLACK-BOOK

if player_verb in ["open"]:
    return print("The book is already open to page 569.")
elif player_verb in ["close", "shut"]:
    return print("As hard as you try, the book cannot be closed.")
elif (
    player_verb in ["turn", "rotate"]
    or player_verb in ["read-page"]
    and context.parser.get_iobj() == _.get_property("intnum")
    and not _.zil_sdk.zstate_get("P-NUMBER") == 569
):
    return print(
        "Beside page 569, there is only one other page with any legible printing on\nit. Most of it is unreadable, but the subject seems to be the banishment of\nevil. Apparently, certain noises, lights, and prayers are efficacious in this\nregard."
    )
elif player_verb in ["burn", "light"]:
    _.zil_sdk.remove(context.parser.get_dobj())
    return _.zil_sdk.jigs_up(
        'A booming voice says "Wrong, cretin!" and you notice that you have\nturned into a pile of dust. How, I can\'t imagine.'
    )
# Auto-generated fallthrough to standard V-<player_verb>
_.zil_sdk.run_v_routine(player_verb)
