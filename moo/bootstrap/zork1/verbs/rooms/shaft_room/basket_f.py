#!moo verb get lower pick raise take --on $raised_basket --dspec either
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: BASKET-F

if player_verb in ["raise"]:
    if _.zil_sdk.zstate_get("CAGE-TOP"):
        return print(_.zil_sdk.pick(_.zil_sdk.zstate_get("DUMMY")))
    else:
        _.zil_sdk.move(_.get_property("raised_basket"), _.get_property("shaft_room"))
        _.zil_sdk.move(_.get_property("lowered_basket"), _.get_property("lower_shaft"))
        _.zil_sdk.zstate_set("CAGE-TOP", True)
        # ZIL: <THIS-IS-IT ...>
        _.zork_thing.invoke_verb("this-is-it", _.get_property("raised_basket"))
        return print("The basket is raised to the top of the shaft.")
elif player_verb in ["lower"]:
    if not _.zil_sdk.zstate_get("CAGE-TOP"):
        return print(_.zil_sdk.pick(_.zil_sdk.zstate_get("DUMMY")))
    else:
        _.zil_sdk.move(_.get_property("raised_basket"), _.get_property("lower_shaft"))
        _.zil_sdk.move(_.get_property("lowered_basket"), _.get_property("shaft_room"))
        # ZIL: <THIS-IS-IT ...>
        _.zork_thing.invoke_verb("this-is-it", _.get_property("lowered_basket"))
        print("The basket is lowered to the bottom of the shaft.")
        _.zil_sdk.zstate_set("CAGE-TOP", None)
        if _.zil_sdk.zstate_get("LIT") and not _.zil_sdk.zstate_set(
            "LIT", _.zork_thing.invoke_verb("lit?", context.player.location)
        ):
            print("It is now pitch black.")
        return True
elif context.parser.get_dobj() == _.get_property("lowered_basket") or context.parser.get_iobj() == _.get_property(
    "lowered_basket"
):
    return print("The basket is at the other end of the chain.")
elif player_verb in ["take", "get", "pick"] and context.parser.get_dobj() in (
    _.get_property("raised_basket"),
    _.get_property("lowered_basket"),
):
    return print("The cage is securely fastened to the iron chain.")
# Auto-generated fallthrough to standard V-<player_verb>
_.zil_sdk.run_v_routine(player_verb)
