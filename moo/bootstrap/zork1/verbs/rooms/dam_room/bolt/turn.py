#!moo verb turn rotate --on "bolt" --dspec either
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison,unnecessary-pass,expression-not-assigned

from moo.sdk import context, lookup
# ZIL routine: BOLT-F (turn branch)

player = context.player
parser = context.parser
the_player_verb = context.parser.words[0].lower() if context.parser is not None and context.parser.words else verb_name

if (parser.get_iobj() if parser.has_iobj() else None) == lookup("wrench"):
    if player.zstate_get("GATE-FLAG"):
        lookup("reservoir_south").set_flag("touchbit", False)
        if player.zstate_get("GATES-OPEN"):
            player.zstate_set("GATES-OPEN", None)
            lookup("loud_room").set_flag("touchbit", False)
            print("The sluice gates close and water starts to collect behind the dam.")
            _.queue("i-rfill", 8)
            # ZIL: <QUEUE ...>
            _.zork_thing.queue(_.zork_thing.i_rempty(), 0)
            return True
        else:
            player.zstate_set("GATES-OPEN", True)
            print("The sluice gates open and water pours through the dam.")
            _.queue("i-rempty", 8)
            # ZIL: <QUEUE ...>
            _.zork_thing.queue(_.zork_thing.i_rfill(), 0)
            return True
    else:
        print("The bolt won't turn with your best effort.")
        return
elif not (parser.get_iobj() if parser.has_iobj() else None) in (lookup("hands"), player.zstate_get("ROOMS")):
    print("The bolt won't turn using the " + (parser.get_iobj() if parser.has_iobj() else None).desc() + ".")
    return
return passthrough()
