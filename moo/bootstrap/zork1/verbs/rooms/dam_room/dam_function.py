#!moo verb close open plug shut --on $dam --dspec either
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: DAM-FUNCTION

if player_verb in ["open", "close", "shut"]:
    return print("Sounds reasonable, but this isn't how.")
elif player_verb in ["plug"]:
    if context.parser.get_iobj() == _.get_property("hands"):
        return print("Are you the little Dutch boy, then? Sorry, this is a big dam.")
    else:
        return print(
            "With a "
            + _.zil_sdk.desc(context.parser.get_iobj())
            + "? Do you know how big this dam is? You could only\nstop a tiny leak with that."
        )
# Auto-generated fallthrough to standard V-<player_verb>
_.zil_sdk.run_v_routine(player_verb)
