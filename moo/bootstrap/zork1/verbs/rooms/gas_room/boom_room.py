#!moo verb burn lamp-on light --on $gas_room --dspec either
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: BOOM-ROOM
# params: RARG
# aux: DUMMY?

rarg = args[0] if len(args) > 0 else None
dummy_p = None

if rarg == "M-END":
    if (
        rarg == "M-END"
        and player_verb in ["lamp-on", "burn", "light"]
        and context.parser.get_dobj() in (_.get_property("candles"), _.get_property("torch"), _.get_property("match"))
    ):
        dummy_p = True
    if (
        _.zork_thing.invoke_verb("held?", _.get_property("candles"))
        and _.zil_sdk.flag(_.get_property("candles"), "onbit")
        or _.zork_thing.invoke_verb("held?", _.get_property("torch"))
        and _.zil_sdk.flag(_.get_property("torch"), "onbit")
        or _.zork_thing.invoke_verb("held?", _.get_property("match"))
        and _.zil_sdk.flag(_.get_property("match"), "onbit")
    ):
        if dummy_p:
            print(
                "How sad for an aspiring adventurer to light a "
                + _.zil_sdk.desc(context.parser.get_dobj())
                + " in a room which\nreeks of gas. Fortunately, there is justice in the world."
            )
        else:
            print(
                "Oh dear. It appears that the smell coming from this room was coal gas.\nI would have thought twice about carrying flaming objects in here."
            )
        return _.zil_sdk.jigs_up("\n\n      ** BOOOOOOOOOOOM **")
# Auto-generated fallthrough to standard V-<player_verb>
_.zil_sdk.run_v_routine(player_verb)
