#!moo verb move press push read --on $blue_button --dspec either
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: BUTTON-F

if player_verb in ["read"]:
    return print("They're greek to you.")
elif player_verb in ["push", "press", "move"]:
    if context.parser.get_dobj() == _.get_property("blue_button"):
        if _.zil_sdk.zstate_get("WATER-LEVEL") == 0:
            _.zil_sdk.set_flag(_.get_property("leak"), "invisible", False)
            print(
                "There is a rumbling sound and a stream of water appears to burst\nfrom the east wall of the room (apparently, a leak has occurred in a\npipe)."
            )
            _.zil_sdk.zstate_set("WATER-LEVEL", 1)
            _.zil_sdk.queue("i-maint-room", -1)
            return True
        else:
            return print("The blue button appears to be jammed.")
    elif context.parser.get_dobj() == _.get_property("red_button"):
        print("The lights within the room ", end="")
        if _.zil_sdk.flag(context.player.location, "onbit"):
            _.zil_sdk.set_flag(context.player.location, "onbit", False)
            return print("shut off.")
        else:
            _.zil_sdk.set_flag(context.player.location, "onbit", True)
            return print("come on.")
    elif context.parser.get_dobj() == _.get_property("brown_button"):
        _.zil_sdk.set_flag(_.get_property("dam_room"), "touchbit", False)
        _.zil_sdk.zstate_set("GATE-FLAG", None)
        return print("Click.")
    elif context.parser.get_dobj() == _.get_property("yellow_button"):
        _.zil_sdk.set_flag(_.get_property("dam_room"), "touchbit", False)
        _.zil_sdk.zstate_set("GATE-FLAG", True)
        return print("Click.")
# Auto-generated fallthrough to standard V-<player_verb>
_.zil_sdk.run_v_routine(player_verb)
