#!moo verb insert place put squeeze --on $tube --dspec either
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: TUBE-FUNCTION

if player_verb in ["put", "place", "insert"] and context.parser.get_iobj() == _.get_property("tube"):
    return print("The tube refuses to accept anything.")
elif player_verb in ["squeeze"]:
    if (
        _.zil_sdk.flag(context.parser.get_dobj(), "open")
        and _.get_property("putty").location == context.parser.get_dobj()
    ):
        _.zil_sdk.move(_.get_property("putty"), context.player)
        return print("The viscous material oozes into your hand.")
    elif _.zil_sdk.flag(context.parser.get_dobj(), "open"):
        return print("The tube is apparently empty.")
    else:
        return print("The tube is closed.")
# Auto-generated fallthrough to standard V-<player_verb>
_.zil_sdk.run_v_routine(player_verb)
