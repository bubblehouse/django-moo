#!moo verb close get lamp-on open pick shut take --on $machine --dspec either
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: MACHINE-F

if player_verb in ["take", "get", "pick"] and context.parser.get_dobj() == _.get_property("machine"):
    return print("It is far too large to carry.")
elif player_verb in ["open"]:
    if _.zil_sdk.flag(_.get_property("machine"), "open"):
        return print(_.zil_sdk.pick(_.zil_sdk.zstate_get("DUMMY")))
    elif _.get_property("machine").contents.first():
        print("The lid opens, revealing ", end="")
        # ZIL: <PRINT-CONTENTS ...>
        _.zork_thing.invoke_verb("print-contents", _.get_property("machine"))
        print(".")
        return _.zil_sdk.set_flag(_.get_property("machine"), "open", True)
    else:
        print("The lid opens.")
        return _.zil_sdk.set_flag(_.get_property("machine"), "open", True)
elif player_verb in ["close", "shut"]:
    if _.zil_sdk.flag(_.get_property("machine"), "open"):
        print("The lid closes.")
        _.zil_sdk.set_flag(_.get_property("machine"), "open", False)
        return True
    else:
        return print(_.zil_sdk.pick(_.zil_sdk.zstate_get("DUMMY")))
elif player_verb in ["lamp-on"]:
    if not context.parser.get_iobj():
        return print("It's not clear how to turn it on with your bare hands.")
    else:
        _.zil_sdk.perform(_.zil_sdk.zstate_get("V?TURN"), _.get_property("machine_switch"), context.parser.get_iobj())
        return True
# Auto-generated fallthrough to standard V-<player_verb>
_.zil_sdk.run_v_routine(player_verb)
