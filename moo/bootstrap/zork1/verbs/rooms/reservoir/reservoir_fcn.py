#!moo verb reservoir-fcn --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: RESERVOIR-FCN
# params: RARG

rarg = args[0] if len(args) > 0 else None

if (
    rarg == "M-END"
    and not _.zil_sdk.flag(context.player.location, "vehicle")
    and not _.zil_sdk.zstate_get("GATES-OPEN")
    and _.zil_sdk.zstate_get("LOW-TIDE")
):
    return print(
        "You notice that the water level here is rising rapidly. The currents\nare also becoming stronger. Staying here seems quite perilous!"
    )
elif rarg == "M-LOOK":
    if _.zil_sdk.zstate_get("LOW-TIDE"):
        print(
            'You are on what used to be a large lake, but which is now a large\nmud pile. There are "shores" to the north and south.',
            end="",
        )
    else:
        print(
            "You are on the lake. Beaches can be seen north and south.\nUpstream a small stream enters the lake through a narrow cleft\nin the rocks. The dam can be seen downstream.",
            end="",
        )
    return print()
