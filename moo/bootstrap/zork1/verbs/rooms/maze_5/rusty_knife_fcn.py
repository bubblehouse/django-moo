#!moo verb attack cut fight get hit kill pick slice stab swing take --on $rusty_knife --dspec either
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: RUSTY-KNIFE-FCN

if player_verb in ["take", "get", "pick"]:
    if _.get_property("sword").location == context.player:
        return print("As you touch the rusty knife, your sword gives a single pulse of blinding\nblue light.")
elif (
    context.parser.get_iobj() == _.get_property("rusty_knife")
    and player_verb in ["attack", "kill", "hit", "fight", "stab", "cut", "slice"]
    or player_verb in ["swing"]
    and context.parser.get_dobj() == _.get_property("rusty_knife")
    and context.parser.get_iobj()
):
    _.zil_sdk.remove(_.get_property("rusty_knife"))
    return _.zil_sdk.jigs_up(
        "As the knife approaches its victim, your mind is submerged by an\novermastering will. Slowly, your hand turns, until the rusty blade\nis an inch from your neck. The knife seems to sing as it savagely\nslits your throat."
    )
# Auto-generated fallthrough to standard V-<player_verb>
_.zil_sdk.run_v_routine(player_verb)
