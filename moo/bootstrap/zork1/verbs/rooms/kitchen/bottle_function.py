#!moo verb break crack destroy mung shake smash throw toss --on $bottle --dspec either
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: BOTTLE-FUNCTION
# aux: E?

e_p = None

if player_verb in ["throw", "toss"] and context.parser.get_dobj() == _.get_property("bottle"):
    _.zil_sdk.remove(context.parser.get_dobj())
    e_p = True
    return print("The bottle hits the far wall and shatters.")
elif player_verb in ["mung", "destroy", "break", "smash", "crack"]:
    e_p = True
    _.zil_sdk.remove(context.parser.get_dobj())
    return print("A brilliant maneuver destroys the bottle.")
elif player_verb in ["shake"]:
    if (
        _.zil_sdk.flag(context.parser.get_dobj(), "open")
        and _.get_property("water").location == context.parser.get_dobj()
    ):
        e_p = True
if e_p and _.get_property("water").location == context.parser.get_dobj():
    print("The water spills to the floor and evaporates.")
    _.zil_sdk.remove(_.get_property("water"))
    return True
elif e_p:
    return True
# Auto-generated fallthrough to standard V-<player_verb>
_.zil_sdk.run_v_routine(player_verb)
