#!moo verb describe examine lamp-off lamp-on throw toss what x --on $lamp --dspec either
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: LANTERN

if player_verb in ["throw", "toss"]:
    print("The lamp has smashed into the floor, and the light has gone out.")
    _.zil_sdk.cancel("i-lantern")
    _.zil_sdk.remove(_.get_property("lamp"))
    return _.zil_sdk.move(_.get_property("broken_lamp"), context.player.location)
elif player_verb in ["lamp-on"]:
    if _.zil_sdk.flag(_.get_property("lamp"), "rmungbit"):
        return print("A burned-out lamp won't light.")
    else:
        return _.zil_sdk.queue("i-lantern", 0)
elif player_verb in ["lamp-off"]:
    if _.zil_sdk.flag(_.get_property("lamp"), "rmungbit"):
        return print("The lamp has already burned out.")
    else:
        return _.zil_sdk.cancel("i-lantern")
elif player_verb in ["examine", "x", "describe", "what"]:
    print("The lamp ", end="")
    if _.zil_sdk.flag(_.get_property("lamp"), "rmungbit"):
        print("has burned out.", end="")
    elif _.zil_sdk.flag(_.get_property("lamp"), "onbit"):
        print("is on.", end="")
    else:
        print("is turned off.", end="")
    return print()
# Auto-generated fallthrough to standard V-<player_verb>
_.zil_sdk.run_v_routine(player_verb)
