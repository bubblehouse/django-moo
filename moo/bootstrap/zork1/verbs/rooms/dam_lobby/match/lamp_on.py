#!moo verb lamp-on burn light --on "matchbook" --dspec either
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison,unnecessary-pass,expression-not-assigned

from moo.sdk import context, lookup
# ZIL routine: MATCH-FUNCTION (lamp-on/burn branch)

player = context.player
parser = context.parser
cnt = 0
the_player_verb = context.parser.words[0].lower() if context.parser is not None and context.parser.words else verb_name

if parser.get_dobj() == this:
    if player.zstate_get("MATCH-COUNT") > 0:
        return player.zstate_set("MATCH-COUNT", player.zstate_get("MATCH-COUNT") - 1)
    if not player.zstate_get("MATCH-COUNT") > 0:
        print("I'm afraid that you have run out of matches.")
        return
    elif player.here() in (lookup("lower_shaft"), lookup("timber_room")):
        print("This room is drafty, and the match goes out instantly.")
        return
    else:
        this.set_flag("flamebit", True)
        this.set_flag("onbit", True)
        _.queue("i-match", 2)
        print("One of the matches starts to burn.")
        if not player.zstate_get("LIT"):
            player.zstate_set("LIT", True)
            # ZIL: <V-LOOK ...>
            _.zork_thing.look()
        return True
return passthrough()
