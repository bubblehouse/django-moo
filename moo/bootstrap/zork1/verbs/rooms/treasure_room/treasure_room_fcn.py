#!moo verb treasure-room-fcn --on $zork_thing
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context, lookup
# ZIL routine: TREASURE-ROOM-FCN
# params: RARG

rarg = args[0] if len(args) > 0 else None

if (
    rarg == "M-ENTER"
    and _.zil_sdk.table_get(
        _.zork_thing.invoke_verb("int", _.zork_thing.invoke_verb("i-thief")), _.zil_sdk.zstate_get("C-ENABLED?")
    )
    == 1
    and not _.zil_sdk.zstate_get("DEAD")
):
    if not lookup("thief").location == context.player.location:
        print(
            "You hear a scream of anguish as you violate the robber's hideaway.\nUsing passages unknown to you, he rushes to its defense."
        )
        _.zil_sdk.move(lookup("thief"), context.player.location)
    _.zil_sdk.set_flag(lookup("thief"), "hostile", True)
    _.zil_sdk.set_flag(lookup("thief"), "invisible", False)
    # ZIL: <THIEF-IN-TREASURE ...>
    return _.zork_thing.invoke_verb("thief-in-treasure")
