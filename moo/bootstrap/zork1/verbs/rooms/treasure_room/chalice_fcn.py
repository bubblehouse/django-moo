#!moo verb get insert pick place put take --on $chalice --dspec either
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context, lookup
# ZIL routine: CHALICE-FCN

if player_verb in ["take", "get", "pick"]:
    if (
        context.parser.get_dobj().location == _.get_property("treasure_room")
        and lookup("thief").location == _.get_property("treasure_room")
        and _.zil_sdk.flag(lookup("thief"), "hostile")
        and not _.zil_sdk.flag(lookup("thief"), "invisible")
        and not _.zil_sdk.getp(lookup("thief"), "description") == _.zil_sdk.zstate_get("ROBBER-U-DESC")
    ):
        return print("You'd be stabbed in the back first.")
elif player_verb in ["put", "place", "insert"] and context.parser.get_iobj() == _.get_property("chalice"):
    return print("You can't. It's not a very good chalice, is it?")
else:
    # ZIL: <DUMB-CONTAINER ...>
    return _.zork_thing.invoke_verb("dumb-container")
# Auto-generated fallthrough to standard V-<player_verb>
_.zil_sdk.run_v_routine(player_verb)
