#!moo verb fill inflate --on $inflatable_boat --dspec either
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: IBOAT-FUNCTION

if player_verb in ["inflate", "fill"]:
    if not _.get_property("inflatable_boat").location == context.player.location:
        return print("The boat must be on the ground to be inflated.")
    elif context.parser.get_iobj() == _.get_property("pump"):
        print("The boat inflates and appears seaworthy.")
        if not _.zil_sdk.flag(_.get_property("boat_label"), "touchbit"):
            print("A tan label is lying inside the boat.")
        _.zil_sdk.zstate_set("DEFLATE", None)
        _.zil_sdk.remove(_.get_property("inflatable_boat"))
        _.zil_sdk.move(_.get_property("inflated_boat"), context.player.location)
        # ZIL: <THIS-IS-IT ...>
        return _.zork_thing.invoke_verb("this-is-it", _.get_property("inflated_boat"))
    elif context.parser.get_iobj() == _.get_property("lungs"):
        return print("You don't have enough lung power to inflate it.")
    else:
        return print("With a " + _.zil_sdk.desc(context.parser.get_iobj()) + "? Surely you jest!")
# Auto-generated fallthrough to standard V-<player_verb>
_.zil_sdk.run_v_routine(player_verb)
