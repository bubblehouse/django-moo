#!moo verb wind --on $broken_canary --dspec either
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: CANARY-OBJECT

if player_verb in ["wind"]:
    if context.parser.get_dobj() == _.get_property("canary"):
        if not _.zil_sdk.zstate_get("SING-SONG") and forest_room_p():
            print(
                "The canary chirps, slightly off-key, an aria from a forgotten opera.\nFrom out of the greenery flies a lovely songbird. It perches on a\nlimb just over your head and opens its beak to sing. As it does so\na beautiful brass bauble drops from its mouth, bounces off the top of\nyour head, and lands glimmering in the grass. As the canary winds\ndown, the songbird flies away."
            )
            _.zil_sdk.zstate_set("SING-SONG", True)
            return _.zil_sdk.move(
                _.get_property("bauble"), cond([["EQUAL?", "HERE", "UP-A-TREE"], "PATH"], ["T", "HERE"])
            )
        else:
            return print("The canary chirps blithely, if somewhat tinnily, for a short time.")
    else:
        return print("There is an unpleasant grinding noise from inside the canary.")
# Auto-generated fallthrough to standard V-<player_verb>
_.zil_sdk.run_v_routine(player_verb)
