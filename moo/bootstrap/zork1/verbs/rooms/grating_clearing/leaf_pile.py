#!moo verb burn count cut get light look-under move pick take --on $leaves --dspec either
# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,pointless-statement,unnecessary-negation,disallowed-name,using-constant-test,redefined-outer-name,no-else-return,unused-variable,redefined-builtin,singleton-comparison
from moo.sdk import context
# ZIL routine: LEAF-PILE

if player_verb in ["count"]:
    return print("There are 69,105 leaves here.")
elif player_verb in ["burn", "light"]:
    # ZIL: <LEAVES-APPEAR ...>
    _.zork_thing.invoke_verb("leaves-appear")
    _.zil_sdk.remove(context.parser.get_dobj())
    if context.parser.get_dobj().location == context.player.location:
        return print("The leaves burn.")
    else:
        return _.zil_sdk.jigs_up("The leaves burn, and so do you.")
elif player_verb in ["cut"]:
    print("You rustle the leaves around, making quite a mess.")
    # ZIL: <LEAVES-APPEAR ...>
    _.zork_thing.invoke_verb("leaves-appear")
    return True
elif player_verb in ["move", "take", "get", "pick"]:
    if player_verb in ["move"]:
        return print("Done.")
    if _.zil_sdk.zstate_get("GRATE-REVEALED"):
        return False
    # ZIL: <LEAVES-APPEAR ...>
    _.zork_thing.invoke_verb("leaves-appear")
    if player_verb in ["take", "get", "pick"]:
        return False
    else:
        return True
elif player_verb in ["look-under"] and not _.zil_sdk.zstate_get("GRATE-REVEALED"):
    return print(
        "Underneath the pile of leaves is a grating. As you release the leaves,\nthe grating is once again concealed from view."
    )
# Auto-generated fallthrough to standard V-<player_verb>
_.zil_sdk.run_v_routine(player_verb)
